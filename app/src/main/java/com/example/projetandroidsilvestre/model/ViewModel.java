package com.example.projetandroidsilvestre.model;

/*import android.app.Application;

import androidx.lifecycle.AndroidViewModel;
import androidx.lifecycle.LiveData;

import com.example.projetandroidsilvestre.model.Picture;

import java.util.List;

public class ViewModel extends AndroidViewModel {

    private Repository mRepository;

    private LiveData<List<Picture>> mAllPictures;

    public ViewModel (Application application) {
        super(application);
        mRepository = new Repository(application);

        mAllPictures = mRepository.getAllPictures();
    }

    LiveData<List<Picture>> getAllPictures() { return mAllPictures; }
    public void insert(Picture picture) { mRepository.insert(picture); }
}*/